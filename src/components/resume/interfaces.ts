export interface SheetProps {
  bgColor: string;
  color?: string;
}

export interface SideBarPorps extends SheetProps {
  width: number;
}

export interface SkillsSliderProps {
  skill: string;
  percent: number;
}

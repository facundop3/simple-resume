import { ResumeData, Action } from "./interfaces";

export const resumeDataREducer = (
  state: ResumeData,
  action: Action
): ResumeData => {
  switch (action.type) {
    default:
      return state;
  }
};
